# 📚 ПОЛНОЕ РУКОВОДСТВО ДЛЯ НАЧИНАЮЩИХ

## 🎯 Что это за проект?

Это веб-приложение для управления школьным расписанием на Django (Python веб-фреймворк) с базой данных PostgreSQL.

**Возможности:**
- Хранение расписания уроков для разных классов
- Добавление/редактирование/удаление уроков через админ-панель
- Быстрое добавление расписания на день
- Личное расписание для учеников
- Глобальное расписание для классов

---

## 📁 Структура проекта

```
school_schedule_project/
│
├── manage.py                    # Главный файл управления Django
│
├── config/                      # Настройки проекта
│   ├── settings.py             # Все настройки (БД, язык, приложения)
│   ├── urls.py                 # Маршруты URL
│   ├── wsgi.py                 # Для production серверов
│   └── asgi.py                 # Для асинхронных серверов
│
├── bot/                        # Главное приложение
│   ├── models.py               # Модели данных (таблицы БД)
│   ├── admin.py                # Настройка админ-панели
│   ├── forms.py                # Формы для ввода
│   ├── views.py                # Логика обработки
│   ├── migrations/             # История изменений БД
│   └── templates/              # HTML шаблоны
│
├── static/                     # CSS, JS, изображения
├── media/                      # Загруженные файлы
└── requirements.txt            # Список библиотек
```

---

## 🔧 Как работает Django?

### Django = фреймворк для веб-сайтов

**Схема работы:**

```
Браузер → URL → View → Model (БД) → Template → HTML → Браузер
```

1. **Пользователь** открывает страницу
2. **Django** смотрит в `urls.py` - куда направить запрос
3. **View** обрабатывает запрос, работает с данными
4. **Model** (ORM) общается с PostgreSQL
5. **Template** создает HTML страницу
6. **Браузер** получает готовую страницу

### Основные компоненты:

**Model** - описывает структуру данных (таблицы БД)
**View** - логика обработки запросов
**Template** - HTML шаблоны
**URL** - маршруты (адреса страниц)

---

## 🚀 Запуск проекта

### Шаг 1: Создать виртуальное окружение

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

### Шаг 2: Установить зависимости

```bash
pip install -r requirements.txt
```

### Шаг 3: Настроить базу данных PostgreSQL

Создайте базу данных в PostgreSQL:

```sql
CREATE DATABASE school_schedule_db;
CREATE USER django_user WITH PASSWORD 'ваш_пароль';
GRANT ALL PRIVILEGES ON DATABASE school_schedule_db TO django_user;
```

### Шаг 4: Настроить переменные окружения

```bash
# Windows
copy .env.example .env

# Linux/Mac
cp .env.example .env
```

Отредактируйте `.env`:

```env
DB_NAME=school_schedule_db
DB_USER=django_user
DB_PASSWORD=ваш_пароль
DB_HOST=localhost
DB_PORT=5432
SECRET_KEY=сгенерировать_командой_ниже
DEBUG=True
```

Генерация SECRET_KEY:
```bash
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

### Шаг 5: Применить миграции

```bash
python manage.py migrate
```

### Шаг 6: Создать администратора

```bash
python manage.py createsuperuser
```

### Шаг 7: Запустить сервер

```bash
python manage.py runserver
```

Откройте: http://localhost:8000/admin/

---

## 🗄️ Модели (Базы данных)

### Что такое модель?

**Модель** = Python класс → таблица в PostgreSQL

### AdminSettings
```python
contact (CharField)  # Контакт администратора
```

### Student - ученики
```python
telegram_id (CharField, unique)    # ID в Telegram
name (CharField)                   # Имя
grade (IntegerField)               # Класс (1-11)
letter (CharField)                 # Буква (А-Г)
use_global (BooleanField)          # Глобальное расписание?
created_at (DateTimeField)         # Дата регистрации
```

### GlobalLesson - глобальное расписание
```python
grade (IntegerField)               # Класс
letter (CharField)                 # Буква
day (IntegerField)                 # День недели (1-5)
number (IntegerField)              # Номер урока
subject (CharField)                # Предмет

unique_together = ['grade', 'letter', 'day', 'number']
```

### PersonalLesson - личное расписание
```python
student (ForeignKey)               # Связь с учеником
day (IntegerField)                 # День недели
number (IntegerField)              # Номер урока
subject (CharField)                # Предмет
```

### Типы полей:

- **CharField** - текст с ограничением длины
- **IntegerField** - целое число
- **BooleanField** - True/False
- **DateTimeField** - дата и время
- **ForeignKey** - связь с другой таблицей

### Параметры:

- `max_length` - максимальная длина
- `unique=True` - значение уникально
- `null=True` - может быть пустым в БД
- `blank=True` - может быть пустым в форме
- `default` - значение по умолчанию
- `verbose_name` - человекочитаемое имя
- `choices` - выбор из списка

---

## 🎨 Админ-панель

### admin.py

Регистрирует модели в админке:

```python
@admin.register(GlobalLesson)
class GlobalLessonAdmin(admin.ModelAdmin):
    list_display = (...)    # Колонки в таблице
    list_filter = (...)     # Фильтры
    search_fields = (...)   # Поиск
```

### Кастомные страницы:

`get_urls()` - добавляет новый URL `/quick-add/`
`quick_add_view()` - обработчик страницы быстрого добавления

---

## 📄 Шаблоны (Templates)

### Синтаксис Django Template:

**Переменные:**
```django
{{ variable }}
{{ student.name }}
{{ lesson.subject }}
```

**Условия:**
```django
{% if has_selection %}
    <div>Выбран класс</div>
{% else %}
    <div>Выберите класс</div>
{% endif %}
```

**Циклы:**
```django
{% for lesson in lessons %}
    <tr>
        <td>{{ lesson.number }}</td>
        <td>{{ lesson.subject }}</td>
    </tr>
{% endfor %}
```

**Наследование:**
```django
{% extends "admin/base_site.html" %}
{% block content %}
    ...
{% endblock %}
```

---

## 🔐 Миграции

**Миграции** = история изменений базы данных

### Процесс:

1. Изменяете `models.py`
2. `python manage.py makemigrations` - создает файл миграции
3. `python manage.py migrate` - применяет к БД

### Команды:

```bash
python manage.py makemigrations    # Создать
python manage.py migrate           # Применить
python manage.py showmigrations    # Статус
```

---

## 🛠️ Полезные команды

### База данных:
```bash
python manage.py migrate           # Применить миграции
python manage.py makemigrations    # Создать миграции
python manage.py flush             # Очистить БД
```

### Администрирование:
```bash
python manage.py createsuperuser   # Создать админа
python manage.py changepassword    # Сменить пароль
```

### Разработка:
```bash
python manage.py runserver         # Запустить сервер
python manage.py shell             # Python консоль
python manage.py collectstatic     # Собрать статику
```

---

## 💡 Как работает код

### 1. Запрос пользователя

```
http://localhost:8000/admin/bot/globallesson/quick-add/
```

### 2. URLs (config/urls.py)

Направляет в админку

### 3. Admin (bot/admin.py)

`get_urls()` добавляет маршрут
`quick_add_view()` обрабатывает запрос

### 4. Forms (bot/forms.py)

Проверяет введенные данные

### 5. Models (bot/models.py)

Сохраняет/получает из PostgreSQL

### 6. Template

Рендерит HTML страницу

### 7. Ответ браузеру

Готовая HTML страница

---

## 🔍 ORM - работа с базой данных

Django ORM позволяет работать с БД через Python код:

### Получить все объекты:
```python
lessons = GlobalLesson.objects.all()
```

### Фильтрация:
```python
lessons = GlobalLesson.objects.filter(
    grade=9,
    letter='А',
    day=1
).order_by('number')
```

### Создать объект:
```python
GlobalLesson.objects.create(
    grade=9,
    letter='А',
    day=1,
    number=1,
    subject='Математика'
)
```

### Получить один объект:
```python
student = Student.objects.get(telegram_id='123456789')
```

### Обновить:
```python
lesson.subject = 'Физика'
lesson.save()
```

### Удалить:
```python
lesson.delete()
```

---

## 📚 Глоссарий

**Django** - веб-фреймворк на Python
**Model** - модель данных (таблица БД)
**View** - обработчик запроса
**Template** - HTML шаблон
**Migration** - изменение структуры БД
**ORM** - работа с БД через объекты
**Admin** - админ-панель Django
**URL** - адрес страницы
**PostgreSQL** - база данных
**Virtual Environment** - изолированное окружение Python

---

## 🐛 Частые проблемы

### `No module named 'django'`
```bash
pip install -r requirements.txt
```

### `password authentication failed`
Проверьте пароль в `.env`

### `port is already in use`
```bash
python manage.py runserver 8080
```

### Забыли пароль админа
```bash
python manage.py changepassword admin
```

---

## 📖 Что изучить дальше

1. Как добавить новую модель
2. Как создать свою страницу (view + template)
3. Django Forms - продвинутые формы
4. Django ORM - сложные запросы
5. Authentication - пользователи и права
6. REST API - создание API
7. Deployment - деплой на сервер

---

**Полезные ссылки:**

- Django документация: https://docs.djangoproject.com/
- PostgreSQL документация: https://www.postgresql.org/docs/
- Python документация: https://docs.python.org/3/

---

**Успехов в изучении! 🚀**
