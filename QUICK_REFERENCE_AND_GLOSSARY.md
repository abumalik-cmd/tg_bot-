# 📖 БЫСТРАЯ СПРАВКА И ГЛОССАРИЙ

---

# РАЗДЕЛ 1: КРАТКИЙ СПРАВОЧНИК ФУНКЦИЙ

## 🔧 Служебные функции

### `get_lesson_time(lesson_number: int) → tuple[str, str]`
**Что делает:** Возвращает время начала и конца урока по его номеру.

```python
get_lesson_time(1)  # → ("08:00", "08:40")
get_lesson_time(9)  # → ("??:??", "??:??")
```

**Параметры:**
- `lesson_number` (int): номер урока (1-8)

**Возвращает:**
- Кортеж (start_time, end_time) в формате "ЧЧ:ММ"

---

### `get_current_lesson_time() → datetime`
**Что делает:** Возвращает текущее время в московском часовом поясе.

```python
now = get_current_lesson_time()
# now = datetime(2026, 2, 18, 14, 30, 45, ..., tzinfo=<MSK>)
now.hour      # 14
now.minute    # 30
now.isoweekday()  # 3 (среда)
```

**Возвращает:**
- `datetime` объект с информацией о текущем времени в Москве

---

### `normalize_letter(letter: str | None) → str | None`
**Что делает:** Проверяет и форматирует букву класса.

```python
normalize_letter("а")     # → "А"
normalize_letter("Б")     # → "Б"
normalize_letter("х")     # → None
normalize_letter(None)    # → None
```

**Параметры:**
- `letter` (str | None): буква класса

**Возвращает:**
- "А" | "Б" | "В" | "Г" если буква валидна
- `None` если буква невалидна или пуста

---

### `get_student_lessons(student: Student, day_num: int) → tuple`
**Что делает:** Получает расписание ученика на конкретный день (глобальное или личное).

```python
student = Student.objects.get(telegram_id=123456789)
lessons, lesson_type = get_student_lessons(student, 1)

# lessons = [<GlobalLesson>, <GlobalLesson>, ...]
# lesson_type = 'global' или 'personal'

for lesson in lessons:
    print(f"{lesson.number}. {lesson.subject}")
```

**Параметры:**
- `student` (Student): объект ученика
- `day_num` (int): день недели (1-5)

**Возвращает:**
- Кортеж (lessons, lesson_type)
- lessons: QuerySet GlobalLesson или PersonalLesson
- lesson_type: 'global' или 'personal'

**Логика:**
1. Если студент выбрал глобальное расписание И у него есть класс и буква
   → Ищет в GlobalLesson
2. Если нашёл → возвращает глобальное
3. Если не нашёл или условия не выполнены → ищет в PersonalLesson

---

### `get_next_lesson(student: Student, current_time: datetime) → tuple`
**Что делает:** Находит следующий урок для ученика.

```python
now = get_current_lesson_time()
student = Student.objects.get(telegram_id=123456789)
next_lesson, next_time = get_next_lesson(student, now)

if next_lesson:
    print(f"Следующий урок: {next_lesson.subject} в {next_time}")
else:
    print("Уроков больше нет")
```

**Параметры:**
- `student` (Student): объект ученика
- `current_time` (datetime): текущее время

**Возвращает:**
- Кортеж (lesson, time_str)
- lesson: объект урока или None
- time_str: время начала урока в формате "ЧЧ:ММ" или None

**Логика:**
1. Проверяет, не выходной ли день (6-7)
2. Получает расписание на сегодня
3. Итерирует по урокам
4. Возвращает первый урок, который ещё не начался
5. Если все уроки прошли → (None, None)

---

## 🎨 Функции создания клавиатур

### `get_main_keyboard() → ReplyKeyboardMarkup`
**Что делает:** Создаёт главное меню с кнопками.

```python
markup = get_main_keyboard()
# Содержит: Сегодня, Завтра, Неделя, Сл. урок, Настройки, Информация
# Расположение: 2 кнопки в строке
```

**Возвращает:**
- `ReplyKeyboardMarkup` объект с 6 кнопками

---

### `get_settings_keyboard(student: Student) → ReplyKeyboardMarkup`
**Что делает:** Создаёт меню настроек.

```python
student = Student.objects.get(telegram_id=123456789)
markup = get_settings_keyboard(student)
# Содержит: Сменить класс, Тип расписания, Назад в меню
```

**Параметры:**
- `student` (Student): объект ученика (не используется в текущей версии)

**Возвращает:**
- `ReplyKeyboardMarkup` объект с 3 кнопками

---

### `get_back_keyboard() → ReplyKeyboardMarkup`
**Что делает:** Создаёт кнопку для возврата в главное меню.

```python
markup = get_back_keyboard()
# Содержит: одна кнопка "Назад в меню"
```

**Возвращает:**
- `ReplyKeyboardMarkup` объект с одной кнопкой

---

## 🤖 Обработчики команд (Message Handlers)

### `@bot.message_handler(commands=['start'])`
**Что делает:** Обработчик команды `/start`.

**Параметры:**
- Первый вызов от пользователя
- Новый пользователь или без выбранного класса → показываем выбор класса
- Существующий пользователь с классом → показываем главное меню

**Эффекты:**
- Создаёт или получает ученика в БД
- Отправляет приветственное сообщение

---

### `@bot.message_handler(func=lambda message: message.text == "📅 Сегодня")`
**Что делает:** Показывает расписание на сегодня.

**Логика:**
1. Проверяет, не выходной ли день
2. Получает расписание ученика на сегодня
3. Определяет статус каждого урока (⏳ ещё не, 🔴 идёт, ✅ прошёл)
4. Отправляет форматированное сообщение

---

### `@bot.message_handler(func=lambda message: message.text == "📆 Завтра")`
**Что делает:** Показывает расписание на завтра.

**Логика:**
1. Рассчитывает день завтра
2. Если завтра суббота → показывает сообщение про выходной
3. Если завтра воскресенье → переходит на понедельник
4. Показывает расписание

---

### `@bot.message_handler(func=lambda message: message.text == "📚 Неделя")`
**Что делает:** Показывает расписание на неделю.

**Логика:**
1. Итерирует по дням ПН-ПТ
2. Для каждого дня получает расписание
3. Форматирует в одном сообщении

---

### `@bot.message_handler(func=lambda message: message.text == "⏰ Следующий урок")`
**Что делает:** Показывает следующий урок с учётом текущего времени.

**Логика:**
1. Находит следующий урок
2. Вычисляет минуты до начала
3. Если менее 10 минут → показывает предупреждение

---

## 🔐 Обработчики callback (Callback Query Handlers)

### `@bot.callback_query_handler(func=lambda call: True)`
**Что делает:** Обработчик нажатия на встроенные кнопки.

**Типы callback_data:**
- `grade_N` (N = 1-11): выбор класса
- `letter_X` (X = А, Б, В, Г): выбор буквы
- `use_global`: включить глобальное расписание
- `use_personal`: включить личное расписание
- `add_personal`: создать личное расписание
- `about_simple`: информация о боте
- `about_tech`: техническая информация

---

## 🔄 Фоновые потоки

### `def reminder_checker()`
**Что делает:** Отправляет напоминания за 10 минут до урока.

**Логика:**
```
Бесконечный цикл:
├─ Каждые 30 сек проверяет текущее время
├─ Если день ПН-ПТ:
│  ├─ Для каждого ученика:
│  │  ├─ Получает расписание на сегодня
│  │  ├─ Для каждого урока:
│  │  │  ├─ Рассчитывает время напоминания (начало - 10 мин)
│  │  │  ├─ Если сейчас это время:
│  │  │  │  └─ Отправляет напоминание
│  │  │  └─ Продолжает проверку
│  │  └─ Продолжает для следующего ученика
│  └─ Ждёт 30 сек перед следующей проверкой
└─ Повторяет
```

---

### `def morning_sender()`
**Что делает:** Отправляет расписание на день в 6:30 утра.

**Логика:**
```
Бесконечный цикл:
├─ Каждую минуту проверяет время
├─ Если 6:30 AM:
│  ├─ Если день ПН-ПТ:
│  │  ├─ Для каждого ученика:
│  │  │  ├─ Получает расписание на сегодня
│  │  │  ├─ Если уроки есть:
│  │  │  │  └─ Отправляет расписание
│  │  │  └─ Продолжает для следующего ученика
│  │  └─ Ждёт 60 сек
│  └─ Если выходной:
│     └─ Пропускает рассылку
└─ Повторяет
```

---

# РАЗДЕЛ 2: ГЛОССАРИЙ ТЕРМИНОВ

## 📚 ТЕХНИЧЕСКИЕ ТЕРМИНЫ

### **API (Application Programming Interface)**
**Что это:** Интерфейс для взаимодействия между программами.

**Пример:**
```
Telegram Bot API - это набор команд и правил,
которыми мы пользуемся, чтобы общаться с сервером Telegram
```

---

### **Callback (Колбэк)**
**Что это:** Функция, которая вызывается в ответ на какое-то событие.

**Пример:**
```python
@bot.message_handler(...)  # ← Это callback
def handle_something():
    # Эта функция вызовется при наступлении события
    ...
```

---

### **Daemon (Демон)**
**Что это:** Фоновый процесс, который работает независимо и завершается с программой.

**Пример:**
```python
Thread(target=morning_sender, daemon=True).start()
#                              ↑
#                              Это демон - работает в фоне
```

---

### **Database Query (Запрос в БД)**
**Что это:** Команда для получения или изменения данных в базе данных.

**Примеры:**
```python
# SELECT запрос (получение)
student = Student.objects.get(telegram_id=123)

# INSERT запрос (создание)
student = Student.objects.create(telegram_id=123, name='Иван')

# UPDATE запрос (обновление)
student.grade = 10
student.save()

# DELETE запрос (удаление)
student.delete()
```

---

### **Django ORM (Object-Relational Mapping)**
**Что это:** Инструмент для работы с БД через Python объекты вместо написания SQL.

**Преимущества:**
```
❌ НЕУДОБНО (SQL):
SELECT * FROM bot_student WHERE grade = 10

✅ УДОБНО (Django ORM):
Student.objects.filter(grade=10)
```

---

### **Decorator (Декоратор)**
**Что это:** Функция, которая "украшает" другую функцию, добавляя ей функциональность.

**Пример:**
```python
@bot.message_handler(commands=['start'])  # ← Это декоратор
def start(message):  # ← Функция, которую украшают
    ...
```

**Как работает:**
```
После @bot.message_handler(...):
1. Бот регистрирует эту функцию
2. Когда происходит событие → функция вызывается автоматически
3. Пользователь не должен вызывать её вручную
```

---

### **Exception (Исключение)**
**Что это:** Ошибка, которая может произойти во время выполнения программы.

**Пример:**
```python
try:
    student = Student.objects.get(telegram_id=999999999)
except Student.DoesNotExist:
    # Ученика не найдено → обрабатываем ошибку
    print("Ученик не найден!")
```

---

### **Foreign Key (Внешний ключ)**
**Что это:** Связь между таблицами - ссылка из одной таблицы на запись в другой.

**Пример:**
```
Student таблица:          PersonalLesson таблица:
┌─────┐                  ┌────────────┐
│ id  │                  │ student_id │ ← Foreign Key (ссылка на Student)
└─────┘                  └────────────┘
  ↑
  └─ Связаны
```

---

### **HTTP (HyperText Transfer Protocol)**
**Что это:** Протокол для передачи данных через Интернет.

**Как работает с ботом:**
```
1. Пользователь пишет сообщение в Telegram
2. Telegram отправляет HTTP запрос к нашему серверу
   POST https://api.telegram.org/... 
   {
     "message": {
       "chat_id": 123456789,
       "text": "/start"
     }
   }
3. Наша программа получает запрос и обрабатывает его
4. Отправляет HTTP ответ с сообщением для пользователя
```

---

### **Index (Индекс)**
**Что это:** Ускоритель для поиска данных в БД.

**Пример:**
```python
# Без индекса - БД проверяет каждую строку (медленно)
# С индексом - БД находит быстро (как оглавление в книге)

telegram_id = models.CharField(unique=True)
# unique=True автоматически создаёт индекс
```

---

### **JSON (JavaScript Object Notation)**
**Что это:** Формат передачи данных - набор "ключ-значение".

**Пример:**
```json
{
  "chat_id": 123456789,
  "name": "Иван",
  "grade": 10,
  "letter": "А"
}
```

---

### **Lambda (Лямбда функция)**
**Что это:** Безымянная функция на одну строку.

**Пример:**
```python
# Обычная функция
def is_hello(text):
    return text == "hello"

# Lambda функция (то же самое, но короче)
lambda text: text == "hello"

# Использование в коде
@bot.message_handler(func=lambda m: m.text == "hello")
```

---

### **Method (Метод)**
**Что это:** Функция, которая принадлежит объекту класса.

**Пример:**
```python
class Student:
    def __init__(self, name):
        self.name = name
    
    def say_hello(self):  # ← Это метод
        print(f"Привет, я {self.name}")

student = Student("Иван")
student.say_hello()  # Вызываем метод
```

---

### **Migration (Миграция)**
**Что это:** История изменений структуры БД.

**Пример:**
```bash
# Когда меняешь models.py, нужно создать миграцию
python manage.py makemigrations

# Затем применить её в БД
python manage.py migrate
```

---

### **Model (Модель)**
**Что это:** Python класс, который описывает таблицу в БД.

**Пример:**
```python
class Student(models.Model):
    telegram_id = models.CharField(...)
    name = models.CharField(...)
    grade = models.IntegerField(...)
    # ↑ Каждое поле = один столбец в таблице
```

---

### **NULL**
**Что это:** Отсутствие значения в БД.

**Пример:**
```python
# null=True - значение может быть пусто
grade = models.IntegerField(null=True)

# Новый ученик:
Student(telegram_id=123, name="Иван", grade=None)  # ← NULL
```

---

### **ORM (Object-Relational Mapping)**
**Что это:** Преобразование между объектами Python и таблицами БД.

**Пример:**
```
Python код        Django ORM        SQL команда       БД таблица
┌──────────┐     ┌─────────────┐   ┌──────────┐    ┌──────────┐
│ Student  │────→│ Преобразует  │──→│ SELECT * │───→│ student  │
│ object   │     │ в SQL        │   │ FROM ... │    │ таблица  │
└──────────┘     └─────────────┘   └──────────┘    └──────────┘
```

---

### **Polling (Полинг)**
**Что это:** Постоянная проверка, есть ли новые события.

**Пример:**
```python
bot.polling(none_stop=True)
# Бот постоянно спрашивает Telegram сервер:
# "Есть ли новые сообщения? Нет? А сейчас? А сейчас?"
# (Так работает много ботов, но есть более быстрый способ - вебхуки)
```

---

### **QuerySet**
**Что это:** Результат запроса к БД - набор объектов.

**Пример:**
```python
lessons = GlobalLesson.objects.filter(grade=10, letter='А')
# lessons - это QuerySet, содержит список уроков

for lesson in lessons:  # Можем итерировать
    print(lesson.subject)
```

---

### **Regular Expression (Regex)**
**Что это:** Шаблон для поиска текста.

**В нашем коде не используется, но пример:**
```python
import re
pattern = r'\d{1,2}:\d{2}'  # Время в формате ЧЧ:ММ
if re.match(pattern, "09:50"):
    print("Это время!")
```

---

### **SQL (Structured Query Language)**
**Что это:** Язык для работы с базами данных.

**Примеры:**
```sql
SELECT * FROM bot_student WHERE grade = 10
INSERT INTO bot_student (name) VALUES ('Иван')
UPDATE bot_student SET grade = 10 WHERE id = 1
DELETE FROM bot_student WHERE id = 1
```

---

### **Thread (Поток)**
**Что это:** Независимая линия выполнения программы.

**Пример:**
```
Основной поток     Поток 1            Поток 2
├─ бот.polling()  ├─ remminder()     ├─ morning_sender()
├─ Ждёт сообщения ├─ Проверяет время ├─ Проверяет время
├─ Обрабатывает   ├─ Отправляет      ├─ Отправляет
└─ Работает       └─ Работает 24/7   └─ Работает 24/7
```

---

### **Timezone (Часовой пояс)**
**Что это:** Смещение от UTC (Koordinierte Universalzeit).

**Пример:**
```python
import pytz

# UTC время
utc_time = datetime.now(pytz.UTC)  # 11:30 UTC

# Moscow время (UTC+3)
msk_time = datetime.now(pytz.timezone('Europe/Moscow'))  # 14:30 MSK
```

---

### **Token (Токен)**
**Что это:** Секретный ключ для доступа к Telegram API.

**Пример:**
```python
TOKEN = '8540461229:AAEY64b5fB0DkOoP96yS6DE9b4MHHmf0JIQ'
# ⚠️ НИКОГДА не публикуй токен!
```

---

### **Validation (Валидация)**
**Что это:** Проверка того, что данные правильные.

**Пример:**
```python
def normalize_letter(letter):
    if not letter:
        return None  # ← Валидация: пусто?
    letter = letter.upper().strip()
    return letter if letter in ['А', 'Б', 'В', 'Г'] else None
    # ↑ Валидация: буква входит в список допустимых?
```

---

### **Webhook (Вебхук)**
**Что это:** Способ получать уведомления, когда что-то произойдёт.

**Сравнение:**
```
Polling (наш метод):
├─ Бот постоянно спрашивает сервер
├─ Сервер: "Нет новых сообщений"
├─ Бот: "А через 1 сек есть?"
└─ Неэффективно (много пустых запросов)

Webhook (альтернатива):
├─ Бот регистрирует адрес у Telegram
├─ Когда приходит сообщение → Telegram отправляет его на адрес
├─ Бот обрабатывает
└─ Эффективнее (только когда нужно)
```

---

# РАЗДЕЛ 3: БЫСТРЫЙ ПОИСК ПО ЗАДАЧАМ

## 🎯 Я хочу найти...

### ...код, который проверяет текущее время
```python
now = get_current_lesson_time()  # Строка 142
# или
now = datetime.now(MSK_TZ)  # Строка 37
```

---

### ...код, который создаёт или обновляет ученика
```python
student, created = Student.objects.get_or_create(...)  # Строка 112
student.save()  # Строка 495, 510, 525
```

---

### ...код, который отправляет сообщение
```python
bot.send_message(chat_id, text, ...)  # Строка 125, 162, 188, и т.д.
bot.edit_message_text(text, ...)  # Строка 480, 513, 528, и т.д.
bot.reply_to(message, text)  # Строка 687, 713
```

---

### ...код, который создаёт расписание
```python
PersonalLesson.objects.create(...)  # Строка 728-733
GlobalLesson.objects.filter(...)  # Строка 49-52
```

---

### ...код, который удаляет данные
```python
PersonalLesson.objects.filter(...).delete()  # Строка 725
student.delete()  # Для удаления ученика (не в коде, но возможно)
del temp_data[user_id]  # Строка 506, 754
```

---

### ...обработчик команды /start
```python
@bot.message_handler(commands=['start'])
def start(message):  # Строка 107
```

---

### ...обработчик кнопки "Сегодня"
```python
@bot.message_handler(func=lambda message: message.text == "📅 Сегодня")
def handle_today(message):  # Строка 180
```

---

### ...фоновый поток для напоминаний
```python
def reminder_checker():  # Строка 757
    while True:
        # Каждые 30 сек проверяет время
        time.sleep(30)  # Строка 799
```

---

### ...фоновый поток для рассылки
```python
def morning_sender():  # Строка 806
    while True:
        if now.hour == 6 and now.minute == 30:  # Строка 813
            # Отправляет расписание
```

---

### ...главное меню
```python
def show_main_menu(message, student):  # Строка 140
    bot.send_message(...)  # Строка 162
```

---

# РАЗДЕЛ 4: ОШИБКИ И ИХ РЕШЕНИЯ

## ❌ Ошибка: `Student.DoesNotExist`

**Что это:** Ученик с таким ID не найден в БД.

**Причина:**
```python
student = Student.objects.get(telegram_id=user_id)
# Если нет ученика с таким ID → ошибка
```

**Решение:**
```python
try:
    student = Student.objects.get(telegram_id=user_id)
except Student.DoesNotExist:
    bot.send_message(user_id, "❌ Вы не зарегистрированы")
    return
```

---

## ❌ Ошибка: `AttributeError: 'NoneType' object has no attribute ...`

**Что это:** Попытка обратиться к свойству объекта, который равен None.

**Пример:**
```python
next_lesson, _ = get_next_lesson(student, now)
print(next_lesson.subject)  # ← Ошибка! next_lesson может быть None
```

**Решение:**
```python
next_lesson, _ = get_next_lesson(student, now)
if next_lesson:  # ← Проверяем перед использованием
    print(next_lesson.subject)
```

---

## ❌ Ошибка: `KeyError: user_id`

**Что это:** Попытка получить значение словаря по ключу, который не существует.

**Пример:**
```python
data = temp_data[user_id]  # ← Ошибка, если user_id не в temp_data
```

**Решение:**
```python
if user_id in temp_data:
    data = temp_data[user_id]
else:
    # Обработка случая, когда пользователя нет в temp_data
```

---

## ❌ Ошибка: `ValueError: invalid literal for int()`

**Что это:** Попытка преобразовать строку в число, которая не является числом.

**Пример:**
```python
day = int("abc")  # ← Ошибка! "abc" не число
```

**Решение:**
```python
try:
    day = int(message.text.strip())
except ValueError:
    bot.reply_to(message, "❌ Введи число")
```

---

## ❌ Ошибка: Бот не отправляет напоминания

**Возможные причины:**
1. Часовой пояс установлен неправильно (проверить MSK_TZ)
2. Поток reminder_checker не запущен (проверить main())
3. У студентов нет расписания (проверить GlobalLesson или PersonalLesson)
4. Токен неправильный или приватный (проверить TOKEN)

**Отладка:**
```python
# Добавить логирование в reminder_checker
print(f"[DEBUG] Текущее время: {now}")
print(f"[DEBUG] Время напоминания: {reminder_time}")
print(f"[DEBUG] Совпадение? {current_time.hour == reminder_time.hour and current_time.minute == reminder_time.minute}")
```

---

# РАЗДЕЛ 5: РАСШИРЕНИЕ И МОДИФИКАЦИЯ

## 💡 Идеи для расширения

### Идея 1: Оценки и домашнее задание
```python
class Homework(models.Model):
    lesson = models.ForeignKey(GlobalLesson, ...)
    description = models.TextField()
    deadline = models.DateField()

class Grade(models.Model):
    student = models.ForeignKey(Student, ...)
    lesson = models.ForeignKey(GlobalLesson, ...)
    value = models.IntegerField()  # 2-5
```

---

### Идея 2: Уведомления о домашнем задании
```python
def homework_reminder():
    while True:
        now = datetime.now(MSK_TZ)
        
        homeworks = Homework.objects.filter(deadline=now.date())
        for hw in homeworks:
            # Отправить напоминание всем ученикам класса
            ...
        
        time.sleep(3600)  # Раз в час
```

---

### Идея 3: Замены уроков
```python
class ReplaceLesson(models.Model):
    original_lesson = models.ForeignKey(GlobalLesson, ...)
    replaced_lesson = models.ForeignKey(GlobalLesson, ...)
    date = models.DateField()  # На какую дату замена
```

---

### Идея 4: Статистика учащихся
```python
# Сколько учеников используют бота
# Какой класс чаще других спрашивает расписание
# Какие часы пик для запросов
```

---

# ЗАКЛЮЧЕНИЕ

Этот глоссарий содержит все основные термины и понятия, используемые в коде.

**Как использовать:**
- Не понимаешь термин? → Ищи в "РАЗДЕЛ 2"
- Не помнишь функцию? → Ищи в "РАЗДЕЛ 1"
- Хочешь найти код для X? → Ищи в "РАЗДЕЛ 3"
- Пришла ошибка? → Ищи в "РАЗДЕЛ 4"
- Хочешь расширить код? → Ищи в "РАЗДЕЛ 5"

**Удачи в программировании! 🚀**
