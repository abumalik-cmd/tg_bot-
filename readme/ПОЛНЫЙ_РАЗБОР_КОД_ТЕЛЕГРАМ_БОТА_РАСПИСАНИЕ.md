# 📱 ПОЛНЫЙ РАЗБОР КОД ТЕЛЕГРАМ БОТА РАСПИСАНИЕ
## Каждая строка объяснена. Каждая логика разобрана. Никакие пропуски.

---

# ОГЛАВЛЕНИЕ
1. ЧТО ЭТО ЗА БОТ?
2. КАК ВСЁ РАБОТАЕТ (общая картина)
3. СТРОКА ЗА СТРОКОЙ РАЗБОР КОДА
4. БАЗА ДАННЫХ (таблицы, какие данные хранятся)
5. ПРИМЕРЫ: когда пользователь что делает
6. ОШИБКИ И КАК ИХ ЧИНИТЬ

---

# 1. ЧТО ЭТО ЗА БОТ?

Это **Telegram бот для школьного расписания**. Ученик пишет боту → он показывает расписание.

**Что бот умеет:**
- 📅 Показать расписание на сегодня
- 📆 Показать расписание на завтра
- 📚 Показать расписание на неделю
- ⏰ Показать следующий урок
- ⚙️ Менять свой класс (10А, 9Б и т.д.)
- 📬 Отправлять уведомления о новых уроках в 6:30 утра
- 🔔 Отправлять напоминание за 10 минут до урока

---

# 2. КАК ВСЁ РАБОТАЕТ (общая картина)

## Общая схема:

```
ПОЛЬЗОВАТЕЛЬ ПИШЕТ В ТЕЛЕГРАМ:
"Привет, /start"
        ↓
TELEGRAM СЕРВЕР ПОЛУЧАЕТ И ОТПРАВЛЯЕТ НА НАШ СЕРВЕР:
{
  "chat_id": 123456789,
  "text": "/start",
  "from_user": {"first_name": "Иван"}
}
        ↓
НАШ БОТ ПОЛУЧАЕТ ДАННЫЕ И ОБРАБАТЫВАЕТ:
- Видит команду /start
- Вызывает функцию start(message)
- Создаёт ученика в базе данных
- Отправляет сообщение с выбором класса
        ↓
БОТ ОТПРАВЛЯЕТ ОТВЕТ В TELEGRAM:
{
  "method": "sendMessage",
  "chat_id": 123456789,
  "text": "Выбери свой класс:",
  "reply_markup": {...кнопки...}
}
        ↓
TELEGRAM ОТПРАВЛЯЕТ СООБЩЕНИЕ ПОЛЬЗОВАТЕЛЮ
        ↓
ПОЛЬЗОВАТЕЛЬ ВИДИТ СООБЩЕНИЕ С КНОПКАМИ
```

## Как программа организована:

```
ФАЙЛ telegram_bot.py (основной файл - 867 строк)
│
├─ ИМПОРТЫ (строки 1-13)
│  └─ Подключаем библиотеки (telebot, datetime и т.д.)
│
├─ ПЕРЕМЕННЫЕ И КОНСТАНТЫ (строки 9-26)
│  ├─ TOKEN - секретный ключ бота
│  ├─ LESSON_TIMES - во сколько начинаются уроки
│  ├─ REMINDER_MINUTES - напоминать за 10 минут
│  └─ temp_data - временное хранилище данных
│
├─ ФУНКЦИИ (строки 29-671)
│  ├─ get_lesson_time() - время начала/конца урока
│  ├─ get_student_lessons() - расписание ученика
│  ├─ get_next_lesson() - следующий урок
│  ├─ handle_today() - показать сегодняшнее расписание
│  ├─ handle_tomorrow() - показать завтрашнее расписание
│  └─ ... и 20+ других функций
│
├─ ФОНОВЫЕ ПОТОКИ (строки 757-847)
│  ├─ reminder_checker() - отправляет напоминания
│  └─ morning_sender() - отправляет расписание в 6:30
│
└─ ГЛАВНАЯ ФУНКЦИЯ (строки 850-866)
   └─ main() - запускает всё это
```

---

# 3. СТРОКА ЗА СТРОКОЙ РАЗБОР КОДА

## ЧАСТЬ 1: ИМПОРТЫ И ПЕРЕМЕННЫЕ (Строки 1-26)

### Строка 1: `import telebot`

```python
import telebot
```

**Что это:** Подключаем библиотеку для работы с Telegram ботом.

**Почему нужна:** Без неё мы не сможем получать сообщения от Telegram и отправлять ответы.

**Как это работает:**
- Библиотека `telebot` - это готовый код, который умеет:
  - Подключаться к серверам Telegram
  - Получать сообщения (polling)
  - Отправлять сообщения обратно
  - Обрабатывать кнопки

**Аналогия:** Это как купить готовый телефон вместо того, чтобы собирать его с нуля.

---

### Строка 2: `from datetime import datetime, timedelta`

```python
from datetime import datetime, timedelta
```

**Что это:** Подключаем две вещи из встроенной библиотеки Python для работы с датами и временем.

**Что такое `datetime`:**
- Это класс, который хранит информацию о дате И времени одновременно
- Примеры значений:
  ```
  2026-02-18 14:30:45.123456  (18 февраля 2026, 14:30 и 45 секунд)
  2026-02-18 06:30:00         (18 февраля 2026, 6:30 утра)
  ```

**Что такое `timedelta`:**
- Это разница между двумя временами
- Примеры:
  ```
  timedelta(minutes=10)  - 10 минут
  timedelta(hours=1)     - 1 час
  timedelta(days=1)      - 1 день
  ```

**Где используется в коде:**
```python
# Пример 1: Получить текущее время
now = datetime.now(MSK_TZ)  # строка 37, 142

# Пример 2: Вычесть 10 минут от времени урока
start_time = datetime.strptime("09:50", "%H:%M").time()
reminder_time = (datetime.combine(...) - timedelta(minutes=10)).time()
# Если урок в 09:50, напоминание в 09:40
```

---

### Строка 3: `import time`

```python
import time
```

**Что это:** Библиотека для работы со временем в Python.

**Главная функция:** `time.sleep(N)` - остановить программу на N секунд.

**Зачем нужна:** В фоновых потоках (reminder_checker, morning_sender) мы в цикле проверяем время. Если проверять каждую наносекунду - процессор перегреется. Поэтому пауза `time.sleep(30)` - каждые 30 секунд проверяем.

**Пример:**
```python
while True:  # Бесконечный цикл
    now = datetime.now()  # Получаем текущее время
    # Проверяем, пришло ли время отправки
    if now.hour == 6 and now.minute == 30:
        send_schedule()  # Отправляем расписание
    time.sleep(60)  # Ждём 60 секунд перед следующей проверкой
```

Без `time.sleep(60)` программа будет проверять 1 миллион раз в секунду и компьютер зависнет!

---

### Строка 4: `from threading import Thread`

```python
from threading import Thread
```

**Что это:** Подключаем возможность запускать несколько вещей одновременно.

**Понимание потоков (threads):**

Представь, что ты в школе:
```
БЕЗ потоков (как работает обычная программа):
- Учитель математики ведёт урок
- Только после его окончания начинается урок физики
- Остальные предметы ждут в очереди
- Неэффективно!

С потоками (как работает наш бот):
- Учитель математики ведёт урок (ОСНОВНОЙ ПОТОК)
- ОДНОВРЕМЕННО звонит в школу оповещение (ПОТОК 1)
- ОДНОВРЕМЕННО замечает, что пора к следующему кабинету (ПОТОК 2)
- ВСЁ РАБОТАЕТ ОДНОВРЕМЕННО!
```

**В нашем коте:**
```python
Thread(target=morning_sender, daemon=True).start()  # Строка 851
Thread(target=reminder_checker, daemon=True).start()  # Строка 852

# Это создаёт два дополнительных потока, которые работают 24/7:
# Поток 1: Каждую минуту проверяет "сейчас 6:30?"
# Поток 2: Каждые 30 сек проверяет "пришло ли время напоминания?"
# ОСНОВНОЙ ПОТОК: Слушает Telegram и обрабатывает сообщения

# Все три потока работают ОДНОВРЕМЕННО!
```

---

### Строка 5: `import pytz`

```python
import pytz
```

**Что это:** Библиотека для работы с часовыми поясами.

**Проблема без этого:**
```python
now = datetime.now()  # Какое сейчас время?
# Ответ зависит от того, где стоит сервер!
# Если сервер в Америке - может быть 04:30
# Если сервер в Москве - может быть 14:30
# НЕПРАВИЛЬНО!
```

**С pytz:**
```python
MSK_TZ = pytz.timezone('Europe/Moscow')
now = datetime.now(MSK_TZ)  # ВСЕГДА московское время
# Независимо от того, где сервер!
```

**Почему это критично для бота:**
- Рассылка в 6:30 должна быть в 6:30 МОСКОВСКОМ ВРЕМЕНИ
- Напоминание за 10 минут должно быть точное
- Если время неправильное - всё сломается!

---

### Строка 7: `from bot.models import Student, GlobalLesson, PersonalLesson, AdminSettings`

```python
from bot.models import Student, GlobalLesson, PersonalLesson, AdminSettings
```

**Что это:** Подключаем МОДЕЛИ из базы данных (описание таблиц).

**Что такое модели:**
- Это описание структуры данных в базе
- Каждая модель = одна таблица в БД

```
Модель Student (ученики):
┌─────────────────────────────┐
│ Таблица "Student"           │
├─────────────────────────────┤
│ Поля:                       │
│ - telegram_id (номер в ТГ)  │
│ - name (имя)                │
│ - grade (класс: 10)         │
│ - letter (буква: А)         │
│ - use_global (флаг)         │
│ - created_at (дата создания)│
└─────────────────────────────┘

Модель GlobalLesson (глобальное расписание):
┌─────────────────────────────┐
│ Таблица "GlobalLesson"      │
├─────────────────────────────┤
│ Поля:                       │
│ - grade (класс)             │
│ - letter (буква)            │
│ - day (день недели 1-5)     │
│ - number (номер урока 1-8)  │
│ - subject (предмет)         │
└─────────────────────────────┘

И так далее...
```

**Где используется:**
```python
# Пример 1: Создание или получение ученика
student, created = Student.objects.get_or_create(
    telegram_id=user_id,
    defaults={'name': name}
)

# Пример 2: Получение расписания класса 10А на понедельник
lessons = GlobalLesson.objects.filter(
    grade=10,
    letter='А',
    day=1
)
```

---

### Строка 9: `TOKEN = '8540461229:AAEY64b5fB0DkOoP96yS6DE9b4MHHmf0JIQ'`

```python
TOKEN = '8540461229:AAEY64b5fB0DkOoP96yS6DE9b4MHHmf0JIQ'
```

**Что это:** Секретный ключ для доступа к боту.

**Критично важно:**
- Это пароль, который даёт полный доступ к боту!
- Если кто-то узнает этот токен - может управлять ботом!
- НИКОГДА не публикуй в интернете!
- НИКОГДА не пишу в публичный репозиторий на GitHub!

**Как получить свой токен:**
1. Найти в Telegram @BotFather
2. Написать `/newbot`
3. Выполнить инструкции
4. Получить свой токен
5. Подставить сюда вместо этого

**Как это работает:**
```
Наша программа использует TOKEN, чтобы Telegram сказал:
"Эта программа - это бот номер 8540461229"
"Доверяю ей и отправляю ей все сообщения"
```

---

### Строка 10: `bot = telebot.TeleBot(TOKEN)`

```python
bot = telebot.TeleBot(TOKEN)
```

**Что это:** Создаём объект бота, который будет всё делать.

**Как работает:**
```python
# Шаг 1: Создаём бота и даём ему токен
bot = telebot.TeleBot(TOKEN)

# Теперь переменная "bot" это весь наш бот!
# Можем использовать:

bot.send_message(chat_id, text)    # Отправить сообщение
bot.edit_message_text(...)         # Отредактировать сообщение
bot.polling()                      # Слушать Telegram
```

**Аналогия:** Это как создать объект "Телефон" и дать ему номер сети. Теперь он может звонить, слать SMS и т.д.

---

### Строка 12: `temp_data = {}`

```python
temp_data = {}
```

**Что это:** Пустой словарь (dictionary) для хранения временных данных.

**Зачем нужен:**

Когда пользователь создаёт личное расписание - это многошаговый процесс:
```
Шаг 1: "Выбери день (1-5)"
       ← Пользователь пишет: 1

Шаг 2: "Теперь введи предметы"
       ← Пользователь пишет: Математика, Русский, История

Шаг 3: Сохраняем в БД
```

Нужно где-то сохранить, что пользователь выбрал день 1! Это `temp_data`:

```python
# Шаг 1: Пользователь выбирает день
temp_data[user_id] = {'day': 1}

# Шаг 2: Пользователь вводит предметы
data = temp_data[user_id]
data['subjects'] = ['Математика', 'Русский', 'История']

# Шаг 3: Сохраняем в БД и удаляем временные данные
del temp_data[user_id]
```

**Почему не в БД:**
- Это временные данные, существуют только во время диалога
- Зачем сохранять в БД данные, которые через минуту удалятся?
- В памяти программы (temp_data) быстрее!

---

### Строка 13: `MSK_TZ = pytz.timezone('Europe/Moscow')`

```python
MSK_TZ = pytz.timezone('Europe/Moscow')
```

**Что это:** Создаём объект часового пояса Москвы.

**Как использовать:**
```python
# Без часового пояса (неправильно):
now = datetime.now()  # 2026-02-18 14:30:45  - какой пояс?

# С часовым поясом (правильно):
now = datetime.now(MSK_TZ)  # 2026-02-18 14:30:45+03:00  - это Москва!
```

**Примеры в коде:**
```python
# Строка 37
def get_current_lesson_time():
    return datetime.now(MSK_TZ)  # ВСЕГДА московское время

# Строка 142
now = get_current_lesson_time()

# Теперь мы знаем точное время, и можем проверять:
if now.hour == 6 and now.minute == 30:
    # Отправляем расписание (в 6:30 московского времени!)
```

---

### Строки 15-24: `LESSON_TIMES = {...}`

```python
LESSON_TIMES = {
    1: {"start": "08:00", "end": "08:40"},
    2: {"start": "08:50", "end": "09:30"},
    3: {"start": "09:50", "end": "10:30"},
    4: {"start": "10:50", "end": "11:30"},
    5: {"start": "11:40", "end": "12:20"},
    6: {"start": "12:30", "end": "13:10"},
    7: {"start": "13:20", "end": "14:00"},
    8: {"start": "14:10", "end": "14:50"},
}
```

**Что это:** Словарь, где хранится расписание звонков школы.

**Как это работает:**

```python
# Хотим узнать, когда 3-й урок?
lesson_times = LESSON_TIMES[3]
print(lesson_times)
# Вывод: {"start": "09:50", "end": "10:30"}

# Хотим узнать только время начала 3-го урока?
start_time = LESSON_TIMES[3]["start"]
print(start_time)
# Вывод: "09:50"

# Это очень удобно!
```

**Зачем это нужно:**

Когда БОТ получает расписание из БД, он получает предмет и номер урока:
```
Урок: {number: 3, subject: "Английский"}
```

Но ученику нужно знать ВО СКОЛЬКО! Поэтому:
```python
number = 3
start_time = LESSON_TIMES[number]["start"]  # "09:50"
end_time = LESSON_TIMES[number]["end"]      # "10:30"

message = f"3. Английский 09:50-10:30"
```

**Если был бы 9-й урок:**
```python
LESSON_TIMES.get(9)  # None (нету 9-го урока)
```

---

### Строка 26: `REMINDER_MINUTES = 10`

```python
REMINDER_MINUTES = 10
```

**Что это:** Количество минут, за которое отправляется напоминание до урока.

**Как работает:**
```
Урок начинается в 09:50
Напоминание отправляется в 09:40 (09:50 - 10 минут)
```

**Зачем:**
- За 10 минут ученик может подготовиться
- Не упустит урок

**Если нужно изменить:**
```python
REMINDER_MINUTES = 15  # Напоминать за 15 минут
REMINDER_MINUTES = 5   # Напоминать за 5 минут
```

---

## ЧАСТЬ 2: ФУНКЦИИ (строки 29-671)

### Функция 1: `get_lesson_time(lesson_number)` (строки 29-33)

```python
def get_lesson_time(lesson_number):
    lesson = LESSON_TIMES.get(lesson_number)
    if lesson:
        return lesson["start"], lesson["end"]
    return "??:??", "??:??"
```

**Что делает:**
Возвращает время начала и конца урока по номеру.

**Пошаговый разбор:**

```python
# ШАГ 1: Получаем урок из словаря
lesson = LESSON_TIMES.get(lesson_number)

# Если lesson_number = 3:
lesson = LESSON_TIMES.get(3)
# lesson = {"start": "09:50", "end": "10:30"}

# Если lesson_number = 9 (такого урока нет):
lesson = LESSON_TIMES.get(9)
# lesson = None

# ШАГ 2: Проверяем, нашли ли урок
if lesson:  # Если найден
    return lesson["start"], lesson["end"]
    # Возвращаем ("09:50", "10:30")
else:  # Если не найден
    return "??:??", "??:??"
    # Возвращаем ("??:??", "??:??")
```

**Примеры использования:**

```python
# Пример 1: Нормальный урок
start, end = get_lesson_time(3)
print(start)  # 09:50
print(end)    # 10:30

# Пример 2: Несуществующий урок
start, end = get_lesson_time(99)
print(start)  # ??:??
print(end)    # ??:??
```

---

### Функция 2: `get_current_lesson_time()` (строки 36-37)

```python
def get_current_lesson_time():
    return datetime.now(MSK_TZ)
```

**Что делает:**
Возвращает ТЕКУЩЕЕ время в московском часовом поясе.

**Почему отдельная функция:**
- Легче вызывать (`get_current_lesson_time()` вместо `datetime.now(MSK_TZ)`)
- Если потом нужно поменять часовой пояс - меняешь в одном месте
- Удобнее для тестирования

**Примеры использования:**

```python
# Пример 1: Узнать текущее время
now = get_current_lesson_time()
# now = datetime.datetime(2026, 2, 18, 14, 30, 45, ..., tzinfo=<MSK>)

# Пример 2: Узнать час
hour = now.hour  # 14

# Пример 3: Узнать минуты
minute = now.minute  # 30

# Пример 4: Узнать день недели
day = now.isoweekday()  # 3 (среда, где 1=ПН, 2=ВТ, ..., 7=ВС)

# Пример 5: Форматировать как строку
time_str = now.strftime("%H:%M")  # "14:30"
```

---

### Функция 3: `normalize_letter(letter)` (строки 40-44)

```python
def normalize_letter(letter):
    if not letter:
        return None
    letter = letter.upper().strip()
    return letter if letter in ['А', 'Б', 'В', 'Г'] else None
```

**Что делает:**
Проверяет букву класса и возвращает её в правильном формате.

**Пошаговый разбор:**

```python
# ПРИМЕР 1: Маленькая буква "а"
letter = "а"

# Шаг 1: Проверяем, не пуста ли
if not letter:  # "а" не пуста
    return None
# Продолжаем

# Шаг 2: Преобразуем в большую букву и удаляем пробелы
letter = letter.upper().strip()
# letter = "А"

# Шаг 3: Проверяем, входит ли в список допустимых
return letter if letter in ['А', 'Б', 'В', 'Г'] else None
# "А" в списке → return "А"


# ПРИМЕР 2: Неправильная буква "х"
letter = "х"

# Шаг 1: Проверяем, не пуста ли
if not letter:  # "х" не пуста
    return None
# Продолжаем

# Шаг 2: Преобразуем в большую букву
letter = letter.upper().strip()
# letter = "Х"

# Шаг 3: Проверяем, входит ли в список
return letter if letter in ['А', 'Б', 'В', 'Г'] else None
# "Х" НЕ в списке → return None


# ПРИМЕР 3: Пуста строка
letter = ""

# Шаг 1: Проверяем, не пуста ли
if not letter:  # пуста!
    return None  # Сразу возвращаем None
```

**Результаты:**
```python
normalize_letter("а")     # "А"
normalize_letter("Б")     # "Б"
normalize_letter(" в ")   # "В"
normalize_letter("х")     # None (неправильная буква)
normalize_letter("")      # None (пуста)
normalize_letter(None)    # None (пуста)
```

---

### Функция 4: `get_student_lessons(student, day_num)` (строки 47-61)

**ЭТО САМАЯ ВАЖНАЯ ФУНКЦИЯ! РАЗБИРАЕМ ОЧЕНЬ ВНИМАТЕЛЬНО!**

```python
def get_student_lessons(student, day_num):
    if student.use_global and student.grade and student.letter:
        lessons = GlobalLesson.objects.filter(
            grade=student.grade,
            letter=student.letter,
            day=day_num
        ).order_by('number')
        if lessons.exists():
            return lessons, 'global'
    
    lessons = PersonalLesson.objects.filter(
        student=student,
        day=day_num
    ).order_by('number')
    return lessons, 'personal'
```

**Что делает:**
Получает расписание ученика на конкретный день. Может быть либо глобальное (для всего класса), либо личное (только для этого ученика).

**ОЧЕНЬ ВАЖНАЯ ЛОГИКА:**

```
ЕСЛИ студент выбрал глобальное расписание
И у студента заполнены класс и буква
ТОГДА:
  Ищем расписание в таблице GlobalLesson
  Если нашли → возвращаем глобальное
ИНАЧЕ:
  Ищем расписание в таблице PersonalLesson
  Возвращаем личное (даже если оно пусто)
```

**Пошаговый пример:**

```
СЦЕНАРИЙ 1: Студент Иван (класс 10А, выбрал глобальное)

# Шаг 1: Проверяем условие
if student.use_global and student.grade and student.letter:
   student.use_global = True  ✓
   student.grade = 10         ✓
   student.letter = 'А'       ✓
   # ВСЕ верны → входим в блок if

# Шаг 2: Ищем в GlobalLesson
lessons = GlobalLesson.objects.filter(
    grade=10,
    letter='А',
    day=1  # Понедельник
).order_by('number')

# SQL перевод:
# SELECT * FROM bot_globallesson 
# WHERE grade=10 AND letter='А' AND day=1 
# ORDER BY number ASC

# Результат (из БД):
lessons = [
    GlobalLesson(number=1, subject='Математика'),
    GlobalLesson(number=2, subject='Русский язык'),
    GlobalLesson(number=3, subject='Английский'),
]

# Шаг 3: Проверяем, нашли ли что-то
if lessons.exists():  # Да, нашли
    return lessons, 'global'  # Возвращаем с флагом 'global'

# РЕЗУЛЬТАТ: lessons=[...], lesson_type='global'


СЦЕНАРИЙ 2: Студент Мария (use_global=False, выбрала личное)

# Шаг 1: Проверяем условие
if student.use_global and student.grade and student.letter:
   student.use_global = False  ✗
   # Условие НЕ выполнено → пропускаем этот блок

# Шаг 2: Ищем в PersonalLesson (ВСЕ уроки, не глобальные)
lessons = PersonalLesson.objects.filter(
    student=student,  # Мария
    day=1
).order_by('number')

# Результат:
lessons = [
    PersonalLesson(number=1, subject='Физика'),
    PersonalLesson(number=2, subject='Математика'),
]

# Шаг 3: Возвращаем (даже если пусто)
return lessons, 'personal'

# РЕЗУЛЬТАТ: lessons=[...], lesson_type='personal'


СЦЕНАРИЙ 3: Студент без класса

# Шаг 1: Проверяем условие
if student.use_global and student.grade and student.letter:
   student.use_global = True    ✓
   student.grade = None         ✗  <- EMPTY!
   # Условие НЕ выполнено → пропускаем

# Шаг 2: Ищем в PersonalLesson
lessons = PersonalLesson.objects.filter(
    student=student,
    day=1
)

# Студент ничего не добавлял в личное → пусто
lessons = []

# Шаг 3: Возвращаем
return [], 'personal'

# РЕЗУЛЬТАТ: lessons=[], lesson_type='personal'
```

**Как использовать в коде:**

```python
# Получить расписание ученика Ивана на понедельник
student = Student.objects.get(telegram_id=123456789)
lessons, lesson_type = get_student_lessons(student, 1)

# lessons - это список объектов урока
# lesson_type - это строка 'global' или 'personal'

for lesson in lessons:
    print(f"{lesson.number}. {lesson.subject}")

if lesson_type == 'global':
    print("Это глобальное расписание класса")
else:
    print("Это личное расписание студента")
```

---

### Функция 5: `get_next_lesson(student, current_time)` (строки 64-77)

```python
def get_next_lesson(student, current_time):
    today_num = current_time.isoweekday()
    if today_num > 5:
        return None, None
    
    lessons, lesson_type = get_student_lessons(student, today_num)
    current_time_str = current_time.strftime("%H:%M")
    
    for lesson in lessons:
        start_time, _ = get_lesson_time(lesson.number)
        if start_time > current_time_str:
            return lesson, start_time
    
    return None, None
```

**Что делает:**
Находит ПЕРВЫЙ урок, который ещё не начался.

**Пошаговый пример:**

```
СЦЕНАРИЙ: Текущее время 10:45 (среда)
Расписание на среду:
- 1. Математика (08:00-08:40)
- 2. Русский (08:50-09:30)
- 3. Английский (09:50-10:30)  ← уже закончился
- 4. История (10:50-11:30)      ← это тот что нужен!
- 5. Физика (11:40-12:20)

# Шаг 1: Получить день недели
today_num = current_time.isoweekday()
# current_time = 2026-02-18 10:45 (среда)
# today_num = 3 (среда)

# Шаг 2: Проверить, не выходной ли день
if today_num > 5:  # 3 > 5? Нет
    return None, None
# Продолжаем (это будни)

# Шаг 3: Получить расписание на этот день
lessons, lesson_type = get_student_lessons(student, 3)
# lessons = [Матем, Русский, Англ, История, Физика]

# Шаг 4: Преобразовать текущее время в строку
current_time_str = current_time.strftime("%H:%M")
# current_time_str = "10:45"

# Шаг 5: Искать первый урок, который еще не начался
for lesson in lessons:
    # ИТЕРАЦИЯ 1: lesson = Математика
    start_time, _ = get_lesson_time(1)  # "08:00"
    if start_time > current_time_str:  # "08:00" > "10:45"? НЕТ
        # Пропускаем, продолжаем
    
    # ИТЕРАЦИЯ 2: lesson = Русский
    start_time, _ = get_lesson_time(2)  # "08:50"
    if start_time > current_time_str:  # "08:50" > "10:45"? НЕТ
        # Пропускаем
    
    # ИТЕРАЦИЯ 3: lesson = Английский
    start_time, _ = get_lesson_time(3)  # "09:50"
    if start_time > current_time_str:  # "09:50" > "10:45"? НЕТ
        # Пропускаем
    
    # ИТЕРАЦИЯ 4: lesson = История
    start_time, _ = get_lesson_time(4)  # "10:50"
    if start_time > current_time_str:  # "10:50" > "10:45"? ДА!!! ✓
        return lesson, start_time
        # return История, "10:50"
        # Выходим из цикла!

# РЕЗУЛЬТАТ: next_lesson = История, next_time = "10:50"
```

**Как использовать:**

```python
student = Student.objects.get(telegram_id=123456789)
now = get_current_lesson_time()

next_lesson, next_time = get_next_lesson(student, now)

if next_lesson:
    print(f"Следующий урок: {next_lesson.subject} в {next_time}")
else:
    print("Уроков больше нет")
```

---

## ЧАСТЬ 3: ОБРАБОТЧИКИ КОМАНД (как бот отвечает)

### Команда /start (строки 107-137)

Это первое, что пользователь пишет боту.

```python
@bot.message_handler(commands=['start'])
def start(message):
    user_id = message.chat.id
    name = message.from_user.first_name or "Ученик"
    
    student, created = Student.objects.get_or_create(
        telegram_id=user_id,
        defaults={'name': name}
    )
    
    if created or not student.grade:
        # Показываем выбор класса
        ...
    else:
        show_main_menu(message, student)
```

**Пошаговый разбор:**

```
ШАГ 1: Пользователь пишет /start
       ├─ Telegram получает сообщение
       └─ Отправляет на наш сервер

ШАГ 2: Наш бот видит команду /start
       └─ Вызывает функцию start(message)

ШАГ 3: Получаем ID и имя пользователя
       user_id = message.chat.id  # 123456789
       name = message.from_user.first_name or "Ученик"
       # Если у пользователя есть имя - берём его
       # Если нет - используем "Ученик"

ШАГ 4: Создаём или получаем ученика из БД
       student, created = Student.objects.get_or_create(
           telegram_id=user_id,
           defaults={'name': name}
       )
       
       # created = True если это НОВЫЙ ученик
       # created = False если ученик УЖЕ в БД

ШАГ 5: Проверяем, выбрал ли уже класс
       if created or not student.grade:
           # Если новый ИЛИ класс не выбран
           # Показываем выбор класса (кнопки 1-11)
       else:
           # Если знаем класс
           # Показываем главное меню

БД РЕЗУЛЬТАТ:

ДО (ученика нет):
┌────────────────┐
│ Student table  │ (пуста)
└────────────────┘

ПОСЛЕ /start (новый ученик):
┌──────────────────────────────────────┐
│ Student table                        │
├──────────────┬──────────┬────────────┤
│ telegram_id  │ name     │ grade      │
├──────────────┼──────────┼────────────┤
│ 123456789    │ Иван     │ NULL       │
│ (и буква тоже NULL, use_global=True) │
└──────────────────────────────────────┘

На экране пользователя:
┌────────────────────────────────────────┐
│ 🎓 Добро пожаловать!                 │
│ Я помогу тебе                         │
│ ✅ Знать расписание                   │
│ ✅ Не пропускать уроки                │
│                                      │
│ Выбери свой класс:                   │
│ ┌──┬──┬──┬──┬──────────────┐         │
│ │1 │2 │3 │4 │... │ 10 │ 11 │         │
│ └──┴──┴──┴──┴──────────────┘         │
└────────────────────────────────────────┘
```

---

### Когда пользователь нажимает кнопку класса (например, "10")

```python
if call.data.startswith('grade_'):
    grade = int(call.data.split('_')[1])  # Извлекаем номер 10
    temp_data[user_id] = {'grade': grade}  # Сохраняем временно
    
    # Показываем выбор буквы (А, Б, В, Г)
    markup = telebot.types.InlineKeyboardMarkup(row_width=4)
    for letter in ['А', 'Б', 'В', 'Г']:
        markup.add(telebot.types.InlineKeyboardButton(
            letter, 
            callback_data=f"letter_{letter}"
        ))
    
    bot.edit_message_text(
        f"📚 Класс {grade}\n\nВыбери букву:",
        call.message.chat.id,
        call.message.message_id,
        parse_mode='Markdown',
        reply_markup=markup
    )
```

**Пошаговый разбор:**

```
ШАГ 1: Пользователь нажимает кнопку "10"
       callback_data = "grade_10" отправляется боту

ШАГ 2: Проверяем тип callback
       if call.data.startswith('grade_'):  # "grade_10" начинается с "grade_"?
       # ДА → входим в блок

ШАГ 3: Извлекаем номер класса
       call.data.split('_') = ["grade", "10"]
       call.data.split('_')[1] = "10"
       int("10") = 10
       grade = 10

ШАГ 4: Сохраняем временно
       temp_data[user_id] = {'grade': 10}
       # Это нужно для следующего шага (выбор буквы)

ШАГ 5: Создаём кнопки для букв
       markup = ...
       for letter in ['А', 'Б', 'В', 'Г']:
           add(button with callback_data=f"letter_{letter}")
       # Результат:
       # ┌──┬──┬──┬──┐
       # │А │Б │В │Г │
       # └──┴──┴──┴──┘

ШАГ 6: Обновляем сообщение (заменяем выбор класса на выбор буквы)
       bot.edit_message_text(...)
       # Не создаём НОВОЕ сообщение
       # Обновляем СУЩЕСТВУЮЩЕЕ сообщение
```

---

### Когда пользователь нажимает кнопку буквы (например, "А")

```python
elif call.data.startswith('letter_'):
    letter = call.data.split('_')[1]  # "А"
    grade = temp_data[user_id]['grade']  # 10 (из temp_data)
    
    student.grade = grade
    student.letter = letter
    student.save()  # КРИТИЧНО! Сохранить в БД
    
    bot.edit_message_text(
        f"✅ Класс успешно установлен!\n\n📚 Твой класс: *{grade}{letter}*",
        call.message.chat.id,
        call.message.message_id,
        parse_mode='Markdown'
    )
    
    show_main_menu(call.message, student)
    del temp_data[user_id]  # Очищаем временные данные
```

**Пошаговый разбор:**

```
ШАГ 1: Пользователь нажимает "А"
       callback_data = "letter_А"

ШАГ 2: Извлекаем букву
       letter = call.data.split('_')[1]  # "А"

ШАГ 3: Получаем класс из временного хранилища
       grade = temp_data[user_id]['grade']  # 10
       # Это мы сохранили на предыдущем шаге!

ШАГ 4: Обновляем ученика в БД
       student.grade = 10
       student.letter = "А"
       student.save()  # ОБЯЗАТЕЛЬНО!
       # Если забыть .save() - данные не сохранятся в БД!

БД РЕЗУЛЬТАТ:
┌──────────────────────────────────────────┐
│ Student table                            │
├──────────────┬──────────┬────────┬───────┤
│ telegram_id  │ name     │ grade  │ letter│
├──────────────┼──────────┼────────┼───────┤
│ 123456789    │ Иван     │ 10     │ А     │
└──────────────────────────────────────────┘

ШАГ 5: Отправляем подтверждение
       "✅ Класс успешно установлен!"

ШАГ 6: Показываем главное меню
       show_main_menu(...)
       # На экране появляется:
       # Привет, Иван!
       # Класс: 10А
       # Выбери действие:
       # [Сегодня] [Завтра]
       # [Неделя] [Сл. урок]
       # и т.д.

ШАГ 7: Очищаем временные данные
       del temp_data[user_id]
       # Удаляем {'grade': 10} из памяти
```

---

## ЧАСТЬ 4: ФОНОВЫЕ ПОТОКИ (что работает в фоне)

### `reminder_checker()` - напоминания за 10 минут (строки 757-803)

Это отдельный поток, который работает 24/7 и отправляет напоминания.

```python
def reminder_checker():
    print("🔔 Поток напоминаний запущен")
    
    while True:  # Бесконечный цикл
        try:
            now = get_current_lesson_time()
            today_num = now.isoweekday()
            
            if today_num <= 5:  # Только ПН-ПТ
                students = Student.objects.all()
                
                for student in students:
                    try:
                        lessons, _ = get_student_lessons(student, today_num)
                        
                        for lesson in lessons:
                            start_time_str, _ = get_lesson_time(lesson.number)
                            start_time = datetime.strptime(start_time_str, "%H:%M").time()
                            reminder_time = (
                                datetime.combine(datetime.today(), start_time) - 
                                timedelta(minutes=REMINDER_MINUTES)
                            ).time()
                            
                            current_time = now.time()
                            
                            if (current_time.hour == reminder_time.hour and 
                                current_time.minute == reminder_time.minute):
                                
                                text = (
                                    f"⚠️ *НАПОМИНАНИЕ!*\n\n"
                                    f"⏰ Через *{REMINDER_MINUTES} минут* урок:\n\n"
                                    f"📚 *{lesson.number}. {lesson.subject}*\n"
                                    f"🕐 Начало: {start_time_str}\n\n"
                                    f"💼 Пора собираться!"
                                )
                                
                                bot.send_message(student.telegram_id, text, parse_mode='Markdown')
                                print(f"✅ Напоминание: {student.name} - {lesson.subject}")
                    
                    except Exception as e:
                        print(f"❌ Ошибка для {student.telegram_id}: {e}")
            
            time.sleep(30)  # КРИТИЧНО! Пауза 30 сек
        
        except Exception as e:
            print(f"❌ Ошибка в reminder_checker: {e}")
            time.sleep(60)
```

**КАК ЭТО РАБОТАЕТ (ОЧЕНЬ ВАЖНО):**

```
МОМЕНТ 1: 09:39:50 (9:39 и 50 секунд)
└─ Поток проверяет все уроки
   └─ Находит урок в 09:50
   └─ Время напоминания: 09:50 - 10 мин = 09:40
   └─ Сейчас 09:39:50 - не совпадает
   └─ Продолжает работу
└─ time.sleep(30) - ждёт 30 сек

МОМЕНТ 2: 09:40:00 (ровно 09:40)
└─ Поток просыпается и проверяет
   └─ Находит урок в 09:50
   └─ Время напоминания: 09:40
   └─ Сейчас 09:40:00 - СОВПАДАЕТ! ✓
   └─ ОТправляет напоминание всем ученикам
   └─ bot.send_message(student.telegram_id, "⚠️ НАПОМИНАНИЕ...")

МОМЕНТ 3: 09:41:00 (через минуту)
└─ Поток проверяет снова
   └─ Находит урок в 09:50
   └─ Время напоминания: 09:40
   └─ Сейчас 09:41:00 - не совпадает (уже прошло)
   └─ Продолжает работу

РЕЗУЛЬТАТ: Напоминание отправляется ровно ОДН РАЗ в 09:40!
```

**РАСЧЁТ ВРЕМЕНИ НАПОМИНАНИЯ (САМОЕ СЛОЖНОЕ):**

```python
# Урок начинается в 09:50
start_time_str = "09:50"

# ШАГ 1: Преобразовать строку в объект time
start_time = datetime.strptime("09:50", "%H:%M").time()
# start_time = datetime.time(9, 50)  (только время, без даты)

# ШАГ 2: Создать полный datetime (дата + время)
start_datetime = datetime.combine(datetime.today(), start_time)
# start_datetime = datetime.datetime(2026, 2, 18, 9, 50)
#                                     ^ дата           ^ время

# ШАГ 3: Вычесть 10 минут
reminder_datetime = start_datetime - timedelta(minutes=10)
# reminder_datetime = datetime.datetime(2026, 2, 18, 9, 40)

# ШАГ 4: Преобразовать обратно в time (без даты)
reminder_time = reminder_datetime.time()
# reminder_time = datetime.time(9, 40)
```

**СРАВНЕНИЕ ВРЕМЕНИ:**

```python
current_time = now.time()  # Текущее время (например, 09:40:15)
reminder_time = datetime.time(9, 40)  # Время напоминания

if (current_time.hour == reminder_time.hour and 
    current_time.minute == reminder_time.minute):
    # 09:40:15.hour = 9, reminder_time.hour = 9 → 9 == 9 ✓
    # 09:40:15.minute = 40, reminder_time.minute = 40 → 40 == 40 ✓
    # ОБА условия верны → отправляем напоминание!
```

---

### `morning_sender()` - утренняя рассылка в 6:30 (строки 806-847)

```python
def morning_sender():
    print("📬 Поток рассылки запущен")
    
    while True:
        try:
            now = get_current_lesson_time()
            
            if now.hour == 6 and now.minute == 30:
                students = Student.objects.all()
                today_num = now.isoweekday()
                
                if today_num <= 5:  # Только в будни
                    for student in students:
                        try:
                            lessons, lesson_type = get_student_lessons(student, today_num)
                            
                            if lessons:
                                days = {1: "ПОНЕДЕЛЬНИК", 2: "ВТОРНИК", ...}
                                source = "🌍" if lesson_type == 'global' else "👤"
                                
                                text = f"{source} *Доброе утро, {student.name}!*\n\n"
                                text += f"📅 *{days[today_num]}*\n"
                                text += "━━━━━━━━━━━━━━━━━━━━\n\n"
                                
                                for lesson in lessons:
                                    start, end = get_lesson_time(lesson.number)
                                    text += f"*{lesson.number}. {lesson.subject}*\n"
                                    text += f"   ⏰ {start} - {end}\n\n"
                                
                                text += "💪 *Удачного дня!*"
                                
                                bot.send_message(student.telegram_id, text, ...)
                                print(f"✅ Рассылка: {student.name}")
                        
                        except Exception as e:
                            print(f"❌ Ошибка: {e}")
            
            time.sleep(60)
        
        except Exception as e:
            print(f"❌ Ошибка в morning_sender: {e}")
            time.sleep(60)
```

**КАК ЭТО РАБОТАЕТ:**

```
КАЖДУЮ МИНУТУ (каждые 60 сек):
└─ Проверяем текущее время
   └─ Если hour == 6 AND minute == 30
      └─ Это 6:30 утра!
      └─ Для КАЖДОГО ученика:
         ├─ Получаем расписание на сегодня
         ├─ Если есть уроки:
         │  └─ Формируем красивое сообщение
         │  └─ Отправляем боту
         └─ Если нет уроков:
            └─ Ничего не отправляем

ПРИМЕР СООБЩЕНИЯ:
┌──────────────────────────────────┐
│ 🌍 Доброе утро, Иван!           │
│                                  │
│ 📅 ПОНЕДЕЛЬНИК                   │
│ ━━━━━━━━━━━━━━━━━━━━            │
│                                  │
│ 1. Математика                    │
│    ⏰ 08:00 - 08:40             │
│                                  │
│ 2. Русский язык                  │
│    ⏰ 08:50 - 09:30             │
│                                  │
│ 3. История                       │
│    ⏰ 09:50 - 10:30             │
│                                  │
│ 💪 Удачного дня!                 │
└──────────────────────────────────┘
```

---

## ЧАСТЬ 5: БАЗА ДАННЫХ (какие таблицы, какие данные)

### Таблица Student (ученики)

```python
class Student(models.Model):
    telegram_id = models.CharField(max_length=50, unique=True)
    # Номер в Telegram (например, 123456789)
    # unique=True - каждый ID уникален, не может быть дубликатов
    
    name = models.CharField(max_length=100)
    # Имя ученика (например, "Иван")
    
    grade = models.IntegerField(choices=GRADE_CHOICES, null=True, blank=True)
    # Класс (1-11), может быть пусто (null=True)
    
    letter = models.CharField(max_length=1, choices=LETTER_CHOICES, null=True, blank=True)
    # Буква (А, Б, В, Г), может быть пуста
    
    use_global = models.BooleanField(default=True)
    # Смотрит ли ученик глобальное расписание? (True = да, False = нет)
    
    created_at = models.DateTimeField(auto_now_add=True)
    # Когда ученик зарегистрировался (автоматически заполняется)
```

**ПРИМЕР ДАННЫХ В ТАБЛИЦЕ:**

```
┌────┬──────────────┬──────┬────────┬────────┬──────────────────────────┐
│ id │ telegram_id  │ name │ grade  │ letter │ use_global               │
├────┼──────────────┼──────┼────────┼────────┼──────────────────────────┤
│ 1  │ 123456789    │ Иван │ 10     │ А      │ True                     │
│ 2  │ 987654321    │ Мария│ 9      │ Б      │ False                    │
│ 3  │ 555666777    │ Петр │ NULL   │ NULL   │ True                     │
└────┴──────────────┴──────┴────────┴────────┴──────────────────────────┘

Иван: зарегистрирован, класс 10А, смотрит глобальное расписание
Мария: зарегистрирована, класс 9Б, смотрит личное расписание
Петр: зарегистрирован, но класс не выбран
```

---

### Таблица GlobalLesson (глобальное расписание)

```python
class GlobalLesson(models.Model):
    grade = models.IntegerField()         # Класс (10)
    letter = models.CharField(max_length=1)  # Буква (А)
    day = models.IntegerField(choices=DAY_CHOICES)  # День (1-5)
    number = models.IntegerField()        # Номер урока (1-8)
    subject = models.CharField(max_length=100)  # Предмет
```

**ПРИМЕР ДАННЫХ:**

```
┌────┬───────┬────────┬─────┬────────┬────────────────┐
│ id │ grade │ letter │ day │ number │ subject        │
├────┼───────┼────────┼─────┼────────┼────────────────┤
│ 1  │ 10    │ А      │ 1   │ 1      │ Математика     │
│ 2  │ 10    │ А      │ 1   │ 2      │ Русский язык   │
│ 3  │ 10    │ А      │ 1   │ 3      │ Английский     │
│ 4  │ 10    │ А      │ 2   │ 1      │ История        │
│ 5  │ 10    │ Б      │ 1   │ 1      │ Физика         │
└────┴───────┴────────┴─────┴────────┴────────────────┘

Расписание класса 10А на понедельник (day=1):
- 1. Математика
- 2. Русский язык
- 3. Английский

Расписание класса 10А на вторник (day=2):
- 1. История

Расписание класса 10Б на понедельник:
- 1. Физика
```

---

### Таблица PersonalLesson (личное расписание)

```python
class PersonalLesson(models.Model):
    student = models.ForeignKey(Student, ...)  # Ссылка на ученика
    day = models.IntegerField()  # День (1-5)
    number = models.IntegerField()  # Номер урока
    subject = models.CharField(max_length=100)  # Предмет
```

**ПРИМЕР ДАННЫХ:**

```
┌────┬────────────┬─────┬────────┬────────────────┐
│ id │ student_id │ day │ number │ subject        │
├────┼────────────┼─────┼────────┼────────────────┤
│ 1  │ 2          │ 1   │ 1      │ Физика         │
│ 2  │ 2          │ 1   │ 2      │ Математика     │
│ 3  │ 2          │ 2   │ 1      │ Английский     │
└────┴────────────┴─────┴────────┴────────────────┘

Личное расписание Марии (student_id=2):
Понедельник: 1. Физика, 2. Математика
Вторник: 1. Английский

(Это отличается от глобального расписания её класса!)
```

---

# 6. ПРИМЕРЫ: КОГДА ПОЛЬЗОВАТЕЛЬ ЧТО ДЕЛАЕТ

### ПРИМЕР 1: Пользователь пишет /start

```
ЭТАП 1: На экране пользователя
┌──────────────────────────────────────┐
│ [Пишет в чат]                        │
│ /start                               │
└──────────────────────────────────────┘

ЭТАП 2: Telegram получает → отправляет на сервер
message = {
    'chat_id': 123456789,
    'text': '/start',
    'from_user': {'first_name': 'Иван'}
}

ЭТАП 3: Наш бот обрабатывает
функция start(message) вызывается
├─ user_id = 123456789
├─ name = 'Иван'
├─ student, created = Student.objects.get_or_create(
│   telegram_id=123456789,
│   defaults={'name': 'Иван'}
│  )
│  # created = True (новый ученик)
├─ if created or not student.grade:  # Условие верно
│  └─ Показываем выбор класса

ЭТАП 4: Бот отправляет ответ
сообщение с кнопками 1-11

ЭТАП 5: На экране пользователя
┌──────────────────────────────────────┐
│ 🎓 Добро пожаловать в бот!          │
│ Я помогу тебе:                      │
│ ✅ Всегда знать расписание          │
│ ✅ Не пропускать уроки              │
│ ✅ Получать напоминания             │
│                                      │
│ 📚 Выбери свой класс:               │
│ ┌──┬──┬──┬──┬──────────────┐        │
│ │1 │2 │3 │4 │... │ 10 │ 11 │        │
│ └──┴──┴──┴──┴──────────────┘        │
└──────────────────────────────────────┘

БД РЕЗУЛЬТАТ:
┌──────────────────────────────────┐
│ Таблица Student                  │
├──────────────┬──────┬────────────┤
│ telegram_id  │ name │ grade      │
├──────────────┼──────┼────────────┤
│ 123456789    │ Иван │ NULL       │
└──────────────┴──────┴────────────┘
```

---

### ПРИМЕР 2: Пользователь нажимает "10", потом "А"

```
ЭТАП 1: Пользователь нажимает "10"
callback_data = "grade_10"
bot получает и вызывает handle_callback(call)

ЭТАП 2: Обработка
├─ if call.data.startswith('grade_'):  # True
├─ grade = 10
├─ temp_data[123456789] = {'grade': 10}
├─ Создаём кнопки А, Б, В, Г
└─ Отправляем новое сообщение

На экране:
┌──────────────────────────┐
│ 📚 Класс 10             │
│ Выбери букву:           │
│ ┌──┬──┬──┬──┐           │
│ │А │Б │В │Г │           │
│ └──┴──┴──┴──┘           │
└──────────────────────────┘

ЭТАП 3: Пользователь нажимает "А"
callback_data = "letter_А"

ЭТАП 4: Обработка
├─ if call.data.startswith('letter_'):  # True
├─ letter = 'А'
├─ grade = temp_data[123456789]['grade']  # 10
├─ student.grade = 10
├─ student.letter = 'А'
├─ student.save()  # СОХРАНЯЕМ В БД
├─ del temp_data[123456789]
└─ show_main_menu(...)

БД РЕЗУЛЬТАТ (ОБНОВИЛОСЬ):
┌──────────────┬──────┬────────┬────────┐
│ telegram_id  │ name │ grade  │ letter │
├──────────────┼──────┼────────┼────────┤
│ 123456789    │ Иван │ 10     │ А      │
└──────────────┴──────┴────────┴────────┘

На экране:
┌──────────────────────────────────┐
│ 🏠 Главное меню                 │
│                                  │
│ 👤 Ученик: Иван                 │
│ 🎓 Класс: 10А                   │
│ 📋 Расписание: 🌍 Глобальное   │
│                                  │
│ ⏰ Следующий урок:               │
│ 📚 История в 10:50              │
│                                  │
│ ┌──────────────┬──────────────┐ │
│ │ 📅 Сегодня   │ 📆 Завтра   │ │
│ ├──────────────┼──────────────┤ │
│ │ 📚 Неделя    │ ⏰ Сл. урок │ │
│ ├──────────────┼──────────────┤ │
│ │ ⚙️ Настройки │ ℹ️ Информ. │ │
│ └──────────────┴──────────────┘ │
└──────────────────────────────────┘
```

---

### ПРИМЕР 3: Пользователь нажимает "📅 Сегодня"

```
ЭТАП 1: Пользователь нажимает кнопку "Сегодня"
message.text = "📅 Сегодня"
bot вызывает handle_today(message)

ЭТАП 2: Обработка
├─ user_id = 123456789
├─ student = Student.objects.get(telegram_id=123456789)
│  # Получает Ивана из БД
├─ today_num = get_current_lesson_time().isoweekday()
│  # today_num = 1 (понедельник, предположим)
├─ if today_num > 5:  # 1 > 5? Нет
│  # Не выходной день
├─ lessons, lesson_type = get_student_lessons(student, 1)
│  # Получает из GlobalLesson уроки класса 10А на понедельник
│  # lessons = [Математика, Русский, Английский, ...]
├─ now = get_current_lesson_time().strftime("%H:%M")
│  # now = "10:45" (пример)

ЭТАП 3: Форматирование расписания с статусами
for lesson in lessons:
    start, end = get_lesson_time(lesson.number)
    
    if start > now:       # "08:00" > "10:45"? Нет
        status = " ⏳"     # урок ещё не начался
    elif end < now:       # "08:40" < "10:45"? Да
        status = " ✅"     # урок уже прошёл
    else:
        status = " 🔴"     # урок идёт прямо сейчас
    
    # Результат для каждого урока...

ЭТАП 4: Отправка сообщения на экран

На экране пользователя:
┌────────────────────────────────────┐
│ 📅 ПОНЕДЕЛЬНИК                    │
│ 📋 🌍 Глобальное расписание      │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│                                    │
│ 1. Математика ✅                  │
│    ⏰ 08:00 - 08:40              │
│                                    │
│ 2. Русский язык ✅                │
│    ⏰ 08:50 - 09:30              │
│                                    │
│ 3. Английский ✅                  │
│    ⏰ 09:50 - 10:30              │
│                                    │
│ 4. История 🔴                     │
│    ⏰ 10:50 - 11:30              │
│                                    │
│ 5. Физика ⏳                      │
│    ⏰ 11:40 - 12:20              │
│                                    │
│ [Сегодня] [Завтра] ...           │
└────────────────────────────────────┘

ПОЯСНЕНИЯ:
✅ - урок уже прошёл (end < сейчас)
🔴 - урок идёт сейчас (start <= сейчас <= end)
⏳ - урок ещё не начался (start > сейчас)
```

---

# 7. ОШИБКИ И КАК ИХ ЧИНИТЬ

### Ошибка 1: `Student.DoesNotExist`

```python
student = Student.objects.get(telegram_id=user_id)
# Если ученика нет в БД → ОШИБКА!
```

**Как чинить:**

```python
try:
    student = Student.objects.get(telegram_id=user_id)
except Student.DoesNotExist:
    bot.send_message(user_id, "❌ Вы не зарегистрированы! Напишите /start")
    return
```

---

### Ошибка 2: `AttributeError: 'NoneType' object has no attribute...`

```python
next_lesson, _ = get_next_lesson(student, now)
print(next_lesson.subject)  # Если next_lesson = None → ОШИБКА!
```

**Как чинить:**

```python
next_lesson, _ = get_next_lesson(student, now)
if next_lesson:
    print(next_lesson.subject)
else:
    print("Уроков больше нет")
```

---

### Ошибка 3: Забыл `.save()` при обновлении

```python
student.grade = 10
student.letter = 'А'
# Забыли student.save()!
# Данные НЕ сохранились в БД!
```

**Как чинить:**

```python
student.grade = 10
student.letter = 'А'
student.save()  # ОБЯЗАТЕЛЬНО!
```

---

### Ошибка 4: Бесконечный цикл без паузы

```python
while True:
    # Проверяем каждую наносекунду!
    # Процессор перегреется!
```

**Как чинить:**

```python
while True:
    # ...обработка...
    time.sleep(30)  # ОБЯЗАТЕЛЬНО!
```

---

### Ошибка 5: Бот не отправляет напоминания

**Возможные причины:**

1. **Часовой пояс неправильный**
   ```python
   # Проверить:
   now = get_current_lesson_time()
   print(now)  # Выводим текущее время в консоль
   # Должно быть московское время!
   ```

2. **Поток не запущен**
   ```python
   # В функции main() должно быть:
   Thread(target=reminder_checker, daemon=True).start()
   # Если этой строки нет → не будет напоминаний!
   ```

3. **Нет расписания в БД**
   ```python
   # Проверить таблицу GlobalLesson:
   # Есть ли уроки для этого класса?
   GlobalLesson.objects.filter(grade=10, letter='А').count()
   # Если 0 → уроков нет → напоминать не что!
   ```

4. **Неправильная логика сравнения времени**
   ```python
   # Проверить:
   current_time = datetime.now().time()
   reminder_time = datetime.time(9, 40)
   print(f"Сейчас: {current_time}")
   print(f"Напоминание: {reminder_time}")
   print(f"Совпадение? {current_time.hour == reminder_time.hour and current_time.minute == reminder_time.minute}")
   ```

---

# ИТОГ

Теперь ты знаешь:
- ✅ Как устроен весь бот (общая картина)
- ✅ Каждую строку кода (импорты, переменные, функции)
- ✅ Как работает база данных (какие таблицы, какие данные)
- ✅ Реальные примеры (что происходит когда пользователь что делает)
- ✅ Фоновые потоки (как работают напоминания и рассылка)
- ✅ Типичные ошибки и как их чинить

**Если что непонятно - спрашивай!** 🚀
